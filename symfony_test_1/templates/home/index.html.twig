{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block content %}
<h2>Vaisseaux de Star Trek</h2>

<table>
    <thead>
        <tr>
            <th>Nom</th>
            <th>Prix</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
        {% for produit in produits %}
            <tr>
                <td>{{ produit.name }}</td>
                <td>{{ produit.price }} crédits</td>
                <td>{{ produit.description }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}
